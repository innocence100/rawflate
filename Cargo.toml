[package]
name = "rawflate"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"

[[bin]]
name = "rawflate"
path = "src/main.rs"

[dependencies]
preflate-container = { path = "../preflate-rs/container" }
lepton_jpeg = "0.5.1"
clap = { version = "4", features = ["derive"] }
